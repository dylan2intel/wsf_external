#
# Apache v2 license
# Copyright (C) 2023 Intel Corporation
# SPDX-License-Identifier: Apache-2.0
#

- name: Create the numastat directory
  file:
    path: "{{ wl_logs_dir}}/{{ trace_node.1 }}-{{ itr | default(1) }}-numastat"
    state: directory
  delegate_to: localhost
  become: no
  ignore_errors: yes

- name: Fetch numastat files
  fetch:
    src: "/tmp/{{ wl_namespace }}-{{ trace_node.1 }}-numastat.{{ item.src }}"
    dest: "{{ wl_logs_dir }}/{{ trace_node.1 }}-{{ itr | default(1) }}-numastat/{{ item.dest }}"
    flat: yes
  become: no
  delegate_to: "{{ trace_node.1 }}"
  ignore_errors: yes
  loop:
  - src: logs
    dest: numastat.logs
  - src: start
    dest: TRACE_START
  - src: stop
    dest: TRACE_STOP

- name: Remove any collection files
  file:
    path: "/tmp/{{ wl_namespace }}-{{ trace_node.1 }}-numastat.{{ item }}"
    state: absent
  become: yes
  delegate_to: "{{ trace_node.1 }}"
  ignore_errors: yes
  loop:
  - logs
  - start
  - stop
  - pid

